<Window x:Class="WorkSpeed.DesktopClient.Views.WorkSpeedFastProductivity"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkSpeed.DesktopClient.Views"
        mc:Ignorable="d"
        Title="WorkSpeedFastProductivity" Height="450" Width="800"
        Background="{StaticResource br_Background}"
        >

    <Window.Resources>

        <BooleanToVisibilityConverter x:Key="Converter" />

        <!--<Style x:Key="{x:Type ListBoxItem}" TargetType="ListBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="Foreground" Value="{DynamicResource br_InactiveListBoxItem}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        
                        <Border x:Name="Border"
                                Padding="2"
                                SnapsToDevicePixels="true">
                            <Border.Background>
                                <SolidColorBrush Color="Transparent" />
                            </Border.Background>


                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="MouseOver" >
                                        <Storyboard>
                                            <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(ListBoxItem.Foreground).(SolidColorBrush.Color)">
                                                <EasingColorKeyFrame KeyTime="0" Value="{DynamicResource col_ActiveListBoxItem}" />
                                            </ColorAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="SelectionStates">
                                    <VisualState x:Name="Unselected" />
                                    <VisualState x:Name="Selected">
                                        <Storyboard>
                                            <ColorAnimationUsingKeyFrames 
                                                                          Storyboard.TargetProperty="(ListBoxItem.Foreground).(SolidColorBrush.Color)">
                                                <EasingColorKeyFrame KeyTime="0" Value="{DynamicResource col_ActiveListBoxItem}" />
                                            </ColorAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>


                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>-->

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2.7*" />
        </Grid.ColumnDefinitions>

        <ListBox Background="{StaticResource br_ListBoxBackground}" 
                 FontSize="14"
                 SnapsToDevicePixels="True" FontFamily="Arial Nova"
                 ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                 SelectedIndex="0"
                 >
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Foreground" Value="{StaticResource br_InactiveListBoxItem}" />
                    <Setter Property="OverridesDefaultStyle" Value="True" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border>
                                    <ContentPresenter Content="{TemplateBinding Content}" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource br_ActiveListBoxItem}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBoxItem x:Name="m_MMM" Margin="5,10,5,0" HorizontalAlignment="Left"
                         >
                <TextBlock Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListBoxItem}}}">dfgdfg</TextBlock>
                <!--<StackPanel Height="30" Orientation="Horizontal">
                    --><!--<Path Data="{StaticResource g_ProductsIcon}" 
                          Height="16" Width="24"
                          Stretch="Uniform"
                          VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}, Path=Foreground}"
                          />--><!--
                    <TextBlock VerticalAlignment="Center"
                               Margin="10,0"
                               Text="Импорт номенклатуры"
                               />
                </StackPanel>-->
            </ListBoxItem>
            <ListBoxItem Margin="5,0" HorizontalAlignment="Left"
                            >dsfsd
                <!--<StackPanel Height="30" Orientation="Horizontal">
                    --><!--<Path Data="{StaticResource g_EmployeesIcon}" 
                          Height="16" Width="24"
                          Stretch="Uniform"
                          VerticalAlignment="Center" 
                          Fill="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}, Path=Foreground}"
                    />--><!--
                    <TextBlock VerticalAlignment="Center"
                               Margin="10,0"
                               Text="Импорт сотрудников"
                    />
                </StackPanel>-->
            </ListBoxItem>
            <ListBoxItem Margin="5,0" HorizontalAlignment="Left">
                <TextBlock>dfgdfg</TextBlock>
                <!--<StackPanel Height="30" Orientation="Horizontal">
                    --><!--<Path Data="{StaticResource g_EditIcon}" 
                          Height="16" Width="24"
                          Stretch="Uniform"
                          VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}, Path=Foreground}"
                    />--><!--
                    <TextBlock VerticalAlignment="Center"
                               Margin="10,0"
                               Text="Проверка сотрудников"
                    />
                </StackPanel>-->
            </ListBoxItem>
            <ListBoxItem Margin="5,0" HorizontalAlignment="Left" >
                <TextBlock>dfgdfg</TextBlock>
                <!--<StackPanel Height="30" Orientation="Horizontal">
                    --><!--<Path Data="{StaticResource g_ProductivityIcon}" 
                          Height="16" Width="24"
                          Stretch="Uniform"
                          VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}, Path=Foreground}"
                    />--><!--
                    <TextBlock VerticalAlignment="Center"
                               Margin="10,0"
                               Text="Выработка сотрудников"
                    />
                </StackPanel>-->
            </ListBoxItem>
        </ListBox> 

        <Grid Grid.Column="1">



            <local:FileImportUserControl x:Name="m_Products" Grid.Column="0"
                                         Visibility="Visible"
                                         HorizontalAlignment="Stretch" 
                                         VerticalAlignment="Stretch"
                                         />
            <local:FileImportUserControl x:Name="m_Employees" Grid.Column="0"
                                         Visibility="Hidden"
                                         HorizontalAlignment="Center" 
                                         VerticalAlignment="Center"
                                         Margin="0,0,0,40"
                                         />

        </Grid>


    </Grid>
</Window>
