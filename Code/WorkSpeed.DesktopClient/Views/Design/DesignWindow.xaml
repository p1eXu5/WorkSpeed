<Window x:Class="WorkSpeed.DesktopClient.Views.Design.DesignWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkSpeed.DesktopClient.Views.Design"
        mc:Ignorable="d"
        Title="DesignWindow" Height="450" Width="800">

    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*" MinHeight="50" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Margin="10,5" Height="Auto">
            <TextBlock FontSize="24"
                       HorizontalAlignment="Center"
                       FontWeight="DemiBold"
                       Foreground="{StaticResource br_MainWindow.BackgroundAlternative}"
                       Text="{}}{Binding Header}"
                       ></TextBlock>
        </Border>

        <Grid Grid.Row="1" MaxWidth="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="60" />
                <RowDefinition Height="26"/>
                <RowDefinition Height="3*" />
            </Grid.RowDefinitions>

            <!-- File -->
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        >
                <Border BorderBrush="Blue" 
                        BorderThickness="0,0,0,1" 
                        SnapsToDevicePixels="True"
                        >
                    <TextBlock Text="{Binding FileName}"
                               MinWidth="200"
                               />
                </Border>

                <Button x:Name="m_OpenFileButton" Margin="10,0,0,0" 
                        Style="{StaticResource st_ImageButton}"
                        Command="{Binding OpenCommand}"
                        >
                    <Image Source="{DynamicResource im_OpenFile}" 
                           RenderOptions.EdgeMode="Aliased"
                           RenderOptions.BitmapScalingMode="Fant"
                           />
                </Button>
            </StackPanel>

            <Grid Grid.Row="2" Margin="20, 2" >
                <StackPanel Orientation="Vertical"
                            VerticalAlignment="Center"
                            >
                    <ProgressBar Height="20" Background="{StaticResource br_MainWindow.Background}"></ProgressBar>
                    <Border Margin="0">
                        <TextBlock Text="{Binding Message}" VerticalAlignment="Top" />
                    </Border>
                </StackPanel>

                <Grid.Style>
                    <Style TargetType="Grid">
                        <Style.Triggers>

                            <DataTrigger Binding="{Binding Path=DataContext.IsInProgress, 
                                                    RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" 
                                         Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1.0" Duration="0:0:.4"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0.0" Duration="0:0:.4"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.ExitActions>
                            </DataTrigger>

                    </Style.Triggers>
                    </Style>
                </Grid.Style>
            </Grid>

            <!-- Buttons -->
            <Grid Grid.Row="3" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Button Content="Выход" Width="80"
                        Command="{Binding BackwardCommand}"
                        />

                <Button Grid.Column="1" Content="Далее" Width="80"
                        Command="{Binding ForwardCommand}"
                        >
                </Button>

            </Grid>
        </Grid>
    </Grid>
</Window>
